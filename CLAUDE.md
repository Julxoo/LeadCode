# CLAUDE.md — leadcode

> Generated by LeadCode. Edit freely — this is YOUR project's rules.
> Re-run update-project to refresh after major dependency changes.

## Architecture Overview

- **Validation**: Zod 3.24.0
- **Runtime**: node
- **Project size**: small (~12 source files)

**Structure:**
- src/ → analyzers, templates, tools
- Root dirs: bin, src

**Scripts:**
- `npm run build` → `tsc`
- `npm run dev` → `tsc --watch`
- `npm run start` → `node dist/index.js`

**Codebase patterns:**
- Uses @/ or ~/ path aliases
- 2 large files (>300 lines)

## Zod

- Test rule for Zod

## Conventions

- File naming: camelCase
- Imports use @/ or ~/ path aliases
- Import order: Node builtins → external packages → type imports → relative imports
- Indentation: 2 spaces
- Quotes: double
- New files go in `src/`
